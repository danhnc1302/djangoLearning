import React from "react";
import {
    StyleSheet, Dimensions
} from 'react-native'

const input = (appSettings) => StyleSheet.create({
    textInput: {
        width: "100%",
        height: 35,
        backgroundColor: "white",
        alignItems: 'center',
        justifyContent: 'center'
    }
})

export default input